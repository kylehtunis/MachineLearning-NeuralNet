#include "Classifier.h"

int Classifier::classify(Example& ex) {
	return -1;
}

vector<int> Classifier::classify(DataSet& ds) {
	return *(new vector<int>);
}

void Classifier::train(DataSet& ds) {
	return;
}